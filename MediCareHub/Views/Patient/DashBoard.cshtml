@model MediCareHub.ViewModels.PatientDashboardViewModel

@{
    ViewData["Title"] = "Patient Dashboard";
    Layout = "~/Views/Shared/_PatientLayout.cshtml"; // Reference the patient layout
}

<h1 class="h3 mb-4 text-gray-800">Welcome, @Model.PatientFullName!</h1>

<!-- Upcoming Appointments -->
<h4 class="h4">Upcoming Appointments</h4>
<ul class="list-group mb-4">
    @if (Model?.UpcomingAppointments != null && Model.UpcomingAppointments.Any())
    {
        foreach (var appointment in Model.UpcomingAppointments)
        {
            <li class="list-group-item d-flex justify-content-between align-items-center">
                <strong>Doctor: @appointment.Doctor.User.FullName</strong>
                <span>@appointment.AppointmentDate.ToString("MM/dd/yyyy hh:mm tt")</span>
                <span class="badge badge-success">@appointment.Status</span>
            </li>
        }
    }
    else
    {
        <li class="list-group-item">No upcoming appointments.</li>
    }
</ul>

<!-- Book New Appointment -->
<a href="@Url.Action("ViewDoctors", "Patient")" class="btn btn-primary">Book New Appointment</a>
